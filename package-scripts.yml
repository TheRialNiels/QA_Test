scripts:
  test:
    default: 'mocha --config .mocharc.yml'
    quickTest:
      all: 'nps "test --grep "@quickTest@""'
      successExample: 'nps "test --grep "@successExample@""'
      failureExample: 'nps "test --grep "@failureExample@""'
